services:
  clearindex:
    image: local-clearindex
    build:
      context: ../clearindex
    restart: 'no'
    depends_on:
      database:
        condition: service_healthy
      message-queue:
        condition: service_healthy
    configs:
      - source: clearindex-config
        target: /etc/app/config.yaml
        